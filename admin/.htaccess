# Ne fonctionne pas avec devilbox ?
# Deny access to filenames starting with dot(.)
<FilesMatch "^\.">
Order allow,deny
Deny from all
</FilesMatch>

# Password protect files
AuthName "Restricted area. Admin only."
AuthType Basic
AuthUserFile "/home/chris/Documents/code_simplon/git/devilbox/data/www/comparOperator/htdocs
/admin/.htpasswd"
Require valid-user
